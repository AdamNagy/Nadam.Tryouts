;
"use strict";

(function() {

    NodeIterator.prototype.ToList = function() {

        var list = new Array();
        while ((node = this.nextNode())) {
            list.push(ndoe);
        }

        return list;
    }

})();