import { PrototypeElement } from "../../../prototype-lib/prototype-element";
export declare class AccordionItemElement extends PrototypeElement {
    constructor(parentId: string, index: string, child: HTMLElement, title?: string);
    Open(): void;
}
