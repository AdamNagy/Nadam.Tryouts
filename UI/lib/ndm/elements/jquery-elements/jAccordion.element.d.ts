import { PrototypeElement } from "../../prototype-lib";
export declare class JAccordion extends PrototypeElement {
    constructor();
    WithItem(title: string, item: PrototypeElement): JAccordion;
    Init(): JAccordion;
}
